'use client';

interface OnboardingProviderProps {
  userId: string;
  userCreatedAt: string;
  userRole: string;
  isPro?: boolean;
}

export function OnboardingProvider(_props: OnboardingProviderProps) {
  return null;
}
